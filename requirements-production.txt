# DDSP Neural Cello - Production Dependencies Only
# embracingearth.space - Minimal for audio synthesis

# Core Audio Processing
numpy>=1.24.0

# MIDI Processing (optional but recommended for better MIDI parsing)
mido>=1.3.0

# Note: TensorFlow/DDSP are optional - server works without them
# The hybrid server uses enhanced custom synthesis as fallback

